startCommand:
  type: http
  configSchema:
    type: object
    properties: {}
    description: No configuration required. The AI Agent Index MCP server is public and requires no API keys or credentials.
  exampleConfig: {}